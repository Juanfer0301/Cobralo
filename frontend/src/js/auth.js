document.getElementById("loginForm")?.addEventListener("submit", e => {
  e.preventDefault();
  alert("Login simulado (más adelante va al backend)");
  window.location.href = "dashboard.html";
});
